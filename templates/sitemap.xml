<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for page in pages %}
    <url>
        <loc>{{ page.loc }}</loc>
        {% if page.lastmod %}
        <lastmod>{{ page.lastmod }}</lastmod>
        {% endif %}
        <changefreq>{{ page.changefreq if page.changefreq else 'weekly' }}</changefreq>
        <priority>{{ page.priority if page.priority else '0.8' }}</priority>
    </url>
    {% endfor %}
</urlset>
